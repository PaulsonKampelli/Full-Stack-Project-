{
  "name": "aimonk-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install-backend": "cd backend && pip install -r requirements.txt",
    "dev": "concurrently \"cd backend && python main.py\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
